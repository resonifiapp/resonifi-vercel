import { useEffect } from 'react';

// TEMPORARILY DISABLED - causing 405 errors
// This component was making HEAD requests that the server rejects
export default function ConnectionWatch() {
  return null;
}